{
  "name": "fso-blog",
  "version": "1.0.0",
  "description": "tech stack: NodeJS, Express, MongoDB, React, Redux, Bootstrap",
  "main": ".eslintrc.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf server/dist && cd client && npm run build && mv dist ../server",
    "start": "NODE_ENV=production node server/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.5.2",
    "@fortawesome/free-solid-svg-icons": "^6.5.2",
    "@fortawesome/react-fontawesome": "^0.2.0"
  }
}
